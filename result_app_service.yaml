apiVersion: v1
kind: Service
metadata: 
  name: result_service
  labels:
    name: result_service
    app: voting_app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    name: result_app_pod
    app: voting_app